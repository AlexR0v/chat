{
  "name": "chat",
  "private": true,
  "license": "MIT",
  "scripts": {
    "lint": "mctl lint",
    "test": "mctl test",
    "commit": "mctl commit",
    "format": "mctl format",
    "typecheck": "mctl typecheck",
    "checkcommit": "mctl precommit",
    "site:dev": "yarn workspace @chat/app dev"
  },
  "dependencies": {
    "lerna": "^3.22.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "mctl precommit",
      "commit-msg": "mctl commitmsg"
    }
  },
  "workspaces": [
    "frontend/**/*"
  ]
}
